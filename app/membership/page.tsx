'use client';
import Link from 'next/link';
import { FormEvent, useState } from 'react';
export default function MembershipPage(){const [msg,setMsg]=useState('');const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();setMsg('Submitting...');const payload=Object.fromEntries(new FormData(e.currentTarget).entries());const res=await fetch('/api/membership',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});setMsg(res.ok?'Application submitted.':'Failed to submit application.');if(res.ok)e.currentTarget.reset();};return <><section className='card section' style={{padding:'1rem'}}><h1>Membership</h1><p>Priority support and volume pricing for recurring buyers.</p></section><form className='card' style={{padding:'1rem'}} onSubmit={submit}><label>Contact name</label><input name='contactName' required/><label>Company name</label><input name='companyName' required/><label>Email</label><input name='email' type='email' required/><label>Phone</label><input name='phone' required/><label>Monthly volume</label><input name='monthlyVolume' required/><label>Notes</label><textarea name='notes' rows={4}/><button className='btn'>Submit application</button><p className='small'>{msg}</p></form><p className='small'>Already approved? <Link href='/login'>Go to login</Link></p></>;}
