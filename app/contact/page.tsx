'use client';
import { FormEvent, useState } from 'react';
export default function ContactPage(){const [msg,setMsg]=useState('');const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();setMsg('Submitting...');const payload=Object.fromEntries(new FormData(e.currentTarget).entries());const res=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});setMsg(res.ok?'Message sent successfully.':'Failed to send message.');if(res.ok)e.currentTarget.reset();};return <><h1>Contact</h1><form className='card' style={{padding:'1rem',maxWidth:700}} onSubmit={submit}><label>Name</label><input name='name' required/><label>Company</label><input name='company' required/><label>Email</label><input name='email' type='email' required/><label>Phone</label><input name='phone' required/><label>Message</label><textarea name='message' required rows={5}/><button className='btn'>Send</button><p className='small'>{msg}</p></form></>;}
