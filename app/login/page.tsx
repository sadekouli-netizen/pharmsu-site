'use client';
import { FormEvent, useState } from 'react';
import { signInWithEmailAndPassword, createUserWithEmailAndPassword } from 'firebase/auth';
import { clientAuth } from '@/lib/firebase.client';
export default function LoginPage(){const [mode,setMode]=useState<'login'|'register'>('login');const [msg,setMsg]=useState('');const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();setMsg('');const f=new FormData(e.currentTarget);const email=String(f.get('email'));const password=String(f.get('password'));try{const cred=mode==='login'?await signInWithEmailAndPassword(clientAuth,email,password):await createUserWithEmailAndPassword(clientAuth,email,password);const token=await cred.user.getIdToken();await fetch(`/api/auth/${mode==='login'?'login':'register'}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,companyName:f.get('companyName'),contactName:f.get('contactName'),phone:f.get('phone')})});setMsg('Success.');}catch{setMsg('Authentication failed.');}};return <><h1>{mode==='login'?'Member Login':'Member Register'}</h1><form className='card' style={{padding:'1rem',maxWidth:560}} onSubmit={submit}><label>Email</label><input name='email' type='email' required/><label>Password</label><input name='password' type='password' minLength={6} required/>{mode==='register'?<><label>Company</label><input name='companyName' required/><label>Contact</label><input name='contactName' required/><label>Phone</label><input name='phone' required/></>:null}<button className='btn'>{mode==='login'?'Login':'Create account'}</button><button type='button' className='btn secondary' onClick={()=>setMode(mode==='login'?'register':'login')}>Switch to {mode==='login'?'Register':'Login'}</button><p className='small'>{msg}</p></form></>;}
